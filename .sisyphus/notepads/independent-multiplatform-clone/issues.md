# Issues

## Known Issues from Planning
- Claude session path: `~/.claude/projects/{slug}/*.jsonl` (NO `/sessions/` subdirectory)
- OpenCode path: `~/.local/share/opencode/storage/message/` (not in ~/.opencode/sessions/)
- `agent-*.jsonl` files located in `{uuid}/subagents/` subdirectory
